services:
  - type: web
    name: dreamshot-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: HF_TOKEN
        sync: false
      - key: PAYMENT_LINK
        sync: false